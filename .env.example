# Server Configuration
PORT=3000
NODE_ENV=production

# Optional MongoDB Connection String (if migrating from JSON storage)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/keyboard_warrior?retryWrites=true&w=majority
